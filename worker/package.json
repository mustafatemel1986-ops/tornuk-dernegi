{
  "name": "tornuk-publish",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/worker.js --conditions=worker",
    "deploy": "npm run build && wrangler deploy",
    "dev": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "wrangler": "^4.28.0"
  },
  "dependencies": {
    "@pushforge/builder": "^2.0.5"
  }
}
